# 回归验收清单（可复跑）

> 用途：把《项目方案与进度》里所有「尚未完成 / 待验收」流程收拢成一份**可重复执行**的验收单。每次改动 `规范表达练习.html` 后按本单从头跑一遍。
>
> 权威方案见 `项目方案与进度.md`；本单只负责“怎么验、判据是什么、谁来验”。

## 由谁验、用哪条入口

| 角色 | 负责范围 | 入口 |
| --- | --- | --- |
| **阿楠（真机/真实浏览器）** | 触摸选择、输入法、真实手机视口、AI 真配置批改 | 桌面双击 `启动规范练习.bat` → 浏览器打开 `http://localhost:8080/规范表达练习.html`；手机浏览器打开同一文件 |
| **开发者（静态 + 浏览器手动）** | 语法、题库结构、向导各步入口、离线降级、暗色/窄屏 | `node _dev/check.js` 等静态脚本（见下）+ 入口2 打开后在浏览器里手动推进到对应场景（窄屏/暗色用 DevTools 设备模拟与页面主题按钮） |

**为什么用 `http://` 不用 `file://`**：FSA / IndexedDB 在 `file://` 下行为不同且历史上会卡启动。无头验证一律 `python -m http.server`（`.bat` 已封装）。

### 三条固定入口

1. **静态自检**（开发者，项目根目录运行）：
   - `node _dev/check.js` — 默认同时校验双入口 `规范表达练习.html` + `index.html`：抽取内联 `<script>` 跑 `node --check`（语法）+ 校验每个 `$('#id')`/`$$('#id')` 都能解析到文档里的 id，并对 `index.html` 额外检查跳转桩同步（meta refresh 与兜底链接指向主文件、无内联业务脚本）。退出码 0 = 全过。
   - `node _dev/_validate.js` — 校验 AI 生成题目的同款 schema（默认输入 `_dev/sample-question.json` 虚构示例夹具，也可传入题目 JSON/JS 文件）：切片来自材料原文、`point` 索引合法、必填字段齐；空输入或 ERROR 令退出码 1。内置题库 `SUMMARY_BANK` 已清空，不再是校验对象。
   - `node _dev/behavior.js` — 行为断言：题库切片规则 + API 配置「本地优先」合并/迁移语义（被测代码从 HTML 原样抽取在 node:vm 沙箱执行，失败令退出码 1）。
   - `node _dev/storage.js` — 存储层断言：`<head>` 键名迁移/主题预载、敏感字段过滤、数据文件载入合并、v1 数据迁移、用户材料 CRUD（同上沙箱策略，失败令退出码 1）。
2. **人工全流程**：`启动规范练习.bat` → `http://localhost:8080/规范表达练习.html`。
3. **浏览器场景验证**：入口2 打开后，按各验收项“操作”列手动推进到对应场景。窄屏用浏览器 DevTools 设备模拟（宽 375px）；暗色用页面右上角主题按钮（`#btnTheme`）。需要留证时截图落 `_dev/shots/`。

> 历史说明：旧版清单依赖 `_dev/_shot.html?act=<场景>` 截图台；该文件从未提交进仓库、现已不存在，相关验收项已全部改写为入口2/入口3 的浏览器手动步骤。

> 状态口径（阿楠敏感项）：报告时必须区分 **已写入代码 / 已过语法检查 / 已浏览器实测 / 尚未实测**。本单每项的“实测”指在真实浏览器里点过一遍。

---

## A. 启动与语法

| # | 入口 | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- | --- |
| A1 | node | `node _dev/check.js` | 退出码 0；双入口无语法错、无未解析 `$('#id')` | 退出码 1（打印失败行） |
| A1a | node | 同 A1（跳转桩同步校验） | `index.html` 保持纯跳转桩：meta refresh 与 `<a href>` 都指向 `规范表达练习.html`，无内联业务脚本 | 跳转目标错 / 桩内出现业务逻辑 |
| A2 | node | `node _dev/_validate.js` | 校验对象非空且无 ERROR（切片来自材料、point 索引合法、必填字段齐） | 校验对象为空 / 任何 `[#序号] 原因` ERROR |
| A2a | node | `node _dev/behavior.js` | 退出码 0；题库切片规则与 API 配置「本地优先」断言全过 | 退出码 1（打印失败断言） |
| A2b | node | `node _dev/storage.js` | 退出码 0；键名迁移、敏感字段过滤、v1 迁移、材料 CRUD 断言全过 | 退出码 1（打印失败断言） |
| A3 | 入口2 | 首次打开（清 localStorage） | 不卡“铺纸研墨”，≤８s 进入练习页；console 无页面报错 | 卡启动 / 有红色报错 |
| A4 | 入口2 | 打开练习页进入概括训练 | 答题卡正常渲染，推进到成句步后 `#btnSubmit` 存在 | 空白 / 控制台报错 |

## B. 概括训练闭环（一步一屏向导）

前置：概括训练不再内置来源型材料。先在“我的材料”中导入自己有权使用的材料，由 AI 生成并确认一道题；规范改写题目也由 AI 生成。正常练习必须配置 API，不提供离线替代结果。

| # | 入口 | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- | --- |
| B1 | 入口2 | 通读→找点，选中前 3 片段 | 停在“找点”，“已选 3 处”，选中片段高亮 | 计数不变 / 未高亮 |
| B2 | 入口2 | 由找点推进到归类，填采分点数量=2，勾事实并命名 | 进入“归类·命名”，出现 2 个要点组，可勾选可命名 | 组数不对 / 勾选无反应 |
| B3 | 入口2 | 由归类推进到“成句”，写入答案 | 进入“成句”，字数统计更新，`#btnSubmit` 可用 | 提交按钮始终禁用 |
| B4 | 入口2（AI，真配置） | 提交概括答案 | 出现 AI 逐语义单元批改、四维评分和下一步建议；顶部标注 AI 批改模型 | 回退到离线对照 / 静默失败 / 报错 |
| B5 | 入口2 | 手动做完 1 道概括题 | 五步都带出所需前文（材料可回看），反馈含“结果+证据+下一步” | 需凭记忆回想材料 / 只给分数和参考答案 |
| B6 | 入口2 | 找点误选/漏选明细 >3 条 | 长列表折叠，“展开全部/收起”闭环正确 | 全部铺开无法收起 |
| B7 | 入口2（在线，真配置） | 选择 L1/L2 题进入找点 | 仍显示预切片，连续材料自由划选面板隐藏 | 在线配置后所有难度都变成自由划选 |
| B8 | 入口2（在线，真配置） | 选择 L3 题，通读时长按并点击下方按钮 | 通读区只显示一份可独立滚动的连续材料；选中文字后视口底部固定出现加入按钮；点击后才记录，提交时统一批改 | 通读和找点被迫分屏 / 选段阶段提前揭示答案 / 找不到加入按钮 |
| B8a | 错题本只练错题 | 在 L3 错题材料中长按并点击下方按钮 | 第一次点击只记录待复盘选段，不自动调用 AI；按钮变为“AI 判断复盘选段”后，用户再次点击才请求并反馈具体解释 | 选中即自动请求 AI / 复盘时不结合错因 |
| B8b | 主导航错题本 | 点击“错题本” | 直接进入练习并切换只练错题，导航显示错题数量；空错题本给出明确提示 | 只能在答题卡题头寻找 / 点击无反应 |
| B9 | 入口2（无已确认概括题） | 切换到概括训练 | 明确提示先到“我的材料”导入材料并由 AI 生成题目，不能进入空白答题流程 | 页面报错 / 显示来源型内置题目 |
| B10 | “我的材料” | 生成结果后查看按钮 | 只有“确认结果并练习”一个入口；未确认材料列表不显示练习按钮 | 同时出现“开始练习”和“确认结果” / 未确认可直接练习 |
| B11 | “我的材料” | 粘贴超过 9000 字的长材料 | 出现分段选择器；生成请求只使用选中片段，原始全文仍保存 | 整本材料一次性发送 / 生成后原文丢失 |

## C. 换题 / 重做 / 下一题 / 模式切换（无状态残留）

| # | 入口 | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- | --- |
| C1 | 入口2 | 作答中途（已有选择/命名/答案）点“换一题” | 换新题，找点选择/命名/答案全部清空，回到通读步；手机上不独占一整行 | 残留上一题的选择或答案 / 工具栏占满首屏 |
| C2 | 入口2 | 提交后点“重新作答” | 回到可作答状态，之前选择/答案清空 | 保留旧作答 / 卡在结果页 |
| C3 | 入口2 | 提交后点“下一题” | 进入新题且状态干净，从通读步开始 | 残留 / 报错 |
| C4 | 入口2 | 概括训练 ↔ 规范改写来回切 | 两模式答题卡正确切换，互不串数据 | 串题 / 面板错位 |
| C5 | 入口2 | 顶部模式切换到规范改写 | 规范改写答题卡显示，可输入可提交 | 面板未切换 |
| C6 | 入口2 | 切换概括训练 ↔ 规范改写 | 旧内容立即收起，显示加载占位；新模式内容加载完成后再显示 | 旧模式内容残留 / 没有加载反馈 |

## D. 离线 / AI 降级 / 折叠

| # | 入口 | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- | --- |
| D1 | 入口2（无 API Key） | 进入练习、提交或检查 | 明确提示先配置 AI，不显示内置题目，不生成离线替代结果 | 无配置仍进入题库 / 静默失败 / 伪装成离线反馈 |
| D2 | 入口2（AI 请求失败） | 提交答案或检查文本 | 保留当前答案，明确提示 AI 失败并允许重试 | 清空输入 / 回退到词库或离线对照 |
| D2a | “我的材料” | 生成长材料题目 | 普通请求最多等待 45 秒，材料生成/解析最多等待 75 秒；输出上限由模型与任务自动管理；切片标点或空白差异不误报，结构不对时自动重试 | 固定 30 秒截断 / 字符边界稍有差异就失败 / 要求用户手动调 Max Tokens |
| D3 | 入口2（在线，真配置） | 阿楠用真实 API Key 提交概括题 | AI 返回逐语义单元 `units[]`+`dimensions`+`nextFocus`，顶部“AI 批改 · 模型:XXX” | 用离线结果冒充 AI / 只给一段总评 |
| D4 | 入口2 | 答案逐点诊断 >3 条 | 诊断列表折叠，可展开/收起 | 无法收起 |

## E. 历史记录与旧数据兼容

| # | 入口 | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- | --- |
| E1 | 入口2 | 提交若干题后切“记录(RECORDS)” | 历史保存并展示；离线 summary 显示“对照”，AI summary 才显示 `score/10` | 记录丢失 / 离线也显示分数 |
| E2 | 入口2 | 主导航切到“记录(RECORDS)”页 | 记录/统计/雷达等区块正常渲染，无报错 | 报错 / 空白 |
| E3 | 入口2 | 载入含旧字段的历史数据 | 旧历史不因新 slice 字段报错，能正常展示 | 旧数据触发异常 |
| E4 | 入口2 | 错题本：做错→加入，做全对→移除 | 徽标计数正确增减 | 计数不同步 |

## F. API 配置持久化（计划§五全场景）

前置：设置里填入并保存一份 API Key。逐项验后配置**不得丢失/被覆盖**。

> 逻辑层已机械化：F3（旧文件空配置不覆盖本地）、F8（本地无配置时从文件补充）的合并语义由 `node _dev/behavior.js`（M1–M5/L1–L3）与 `node _dev/storage.js`（S5/S6）断言；敏感字段不进 D.settings（F10 的代码面）由 storage.js（S1/S2/V1）断言。node 断言通过只算“静态检查通过”，浏览器实测（刷新/重开/真实文件句柄）仍以本表为准。

| # | 场景 | ✓ 通过 |
| --- | --- | --- |
| F1 | 保存后刷新页面 | 配置仍在 |
| F2 | 关闭浏览器后重开 | 配置仍在 |
| F3 | 先连接旧数据文件再打开设置 | 本地配置不被旧文件空配置覆盖 |
| F4 | 切离线再切回在线 | 配置仍在 |
| F5 | 清空练习历史 | API 配置不受影响 |
| F6 | 仅改模型 / Base URL 后重开 | 改动保留 |
| F7 | API 请求失败后重开 | 输入框/已存配置未被清空 |
| F8 | localStorage 无配置、旧数据文件有配置 | 从文件补充 |
| F9 | 两边冲突 | 界面明确采用哪份，不静默覆盖 |
| F10 | 全程 | 真实 API Key 不写进仓库/题库/Git 历史/日志 |

## G. 手机端与窄屏（计划§六）

| # | 入口 | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- | --- |
| G1 | 入口3（DevTools 375px） | 入口2 打开后 DevTools 设备模拟宽 375px，跑一遍 AI 批改闭环 | 材料/片段/命名框/答案区零横向溢出 | 出现横向滚动/溢出 |
| G2 | 阿楠真机（Android + iPhone） | 浏览器独立打开、L3 材料滚动、长按选字、点击加入、输入法、保存、重开恢复 | 材料区域可独立滚动；长按后材料下方出现加入按钮；错题本不自动请求 AI；复制/长按不被拦截；提交或主动点击 AI 后流程正常；不依赖桌面 `.bat`/Python | 真机不可用 |
| G3 | 入口3（DevTools 375px） | 375px 模拟下点页面主题按钮切到暗色 | 暗色下三色克制、无错位 | 对比失衡/错位 |

## H. 数据备份：导入 / 导出 / 复制（计划§六）

| # | 操作 | ✓ 通过 | ✗ 未通过 |
| --- | --- | --- | --- |
| H1 | 导出数据为 JSON | 含练习记录/错题/画像/设置；API Key 默认不导出或明确二次确认 | Key 被静默导出 |
| H2 | 从文件导入 JSON | 导入前显示数据时间与记录数并要求确认 | 无确认直接覆盖 |
| H3 | 导入失败 | 现有数据不被破坏 | 失败后数据丢失 |
| H4 | 复制数据（文本兜底） | 可复制为文本 | 复制失败 |

---

## 一轮验收记录模板

复制到验收当日的 `.workbuddy/memory/<日期>.md` 或提交说明里填写：

```
回归验收 @ <日期> / commit <hash> / 方案版本：项目方案与进度.md
入口: node _dev/check.js + node _dev/_validate.js + node _dev/behavior.js + node _dev/storage.js / 启动规范练习.bat / 浏览器手动场景（DevTools 375px + 主题按钮）
A 启动语法:  A1 [ ] A1a [ ] A2 [ ] A2a [ ] A2b [ ] A3 [ ] A4 [ ]
B 概括闭环:  B1 [ ] B2 [ ] B3 [ ] B4 [ ] B5 [ ] B6 [ ]
C 无残留:    C1 [ ] C2 [ ] C3 [ ] C4 [ ] C5 [ ] C6 [ ]
D 离线/AI:   D1 [ ] D2 [ ] D3 [ ] D4 [ ]
E 历史兼容:  E1 [ ] E2 [ ] E3 [ ] E4 [ ]
F API持久化: F1 [ ] F2 [ ] F3 [ ] F4 [ ] F5 [ ] F6 [ ] F7 [ ] F8 [ ] F9 [ ] F10 [ ]
G 手机窄屏:  G1 [ ] G2 [ ] G3 [ ]
H 数据备份:  H1 [ ] H2 [ ] H3 [ ] H4 [ ]
未通过项与结论:
```

> 判据约定：`[✓]`=实测通过；`[✗]`=实测未通过（写明现象）；`[代码]`=已写入未实测；`[N/A]`=本轮不涉及。禁止把“已写入代码”记成“已验证”。
