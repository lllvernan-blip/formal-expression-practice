# 材料与真题导入功能 — 实施记录

> 状态：**已实现** | 修改文件：`规范表达练习.html` | 验证通过：`check.js` + `_validate.js`

## 一、功能概述

在现有"练习 / 扫描 / 记录"导航旁新增"材料"入口，让用户把自己的材料/真题变成练习内容。

### 模式 A：只有材料

用户粘贴或上传一份材料 → 选择训练方向 → AI 生成题目、切片、采分点、参考答案 → 确认后进入概括训练。

标记：来源=用户上传材料 · 题目=AI 生成 · 参考答案=AI 生成，待用户确认

### 模式 B：导入真题

用户上传材料（必填）+ 题目（可选）+ 答案（可选）→ AI 结构化提取 → 用户核对 → 保存 → 练习。

原则：原始材料/题目/答案原样保存，AI 只做结构化提取放在 `aiAnalysis`，不覆盖原文。

## 二、数据结构

独立 localStorage key：`formal_expression_user_materials_v1`

```js
{
  id: 'um_时间戳_随机串',
  mode: 'material' | 'true-question',
  status: 'draft' | 'ready' | 'archived',
  title: '自定义标题',
  createdAt: 'ISO',
  updatedAt: 'ISO',
  original: {
    material: '原始材料文本',
    question: '原始题目文本',
    answer: '原始答案文本',
    files: { materialName, questionName, answerName }
  },
  generated: {
    type, level, domain, prompt, material, slices, points,
    buildChain, answerStructure, ref, aiAnalysis
  },
  provenance: {
    sourceType: '用户上传材料' | '用户导入真题',
    generatedByAI: boolean,
    userConfirmed: boolean,
    confirmedAt: null | ISO
  }
}
```

## 三、AI 输出协议

### 材料模式 → JSON

```json
{ "title", "type", "level", "prompt", "material", "slices", "points", "buildChain", "answerStructure", "ref", "uncertainties" }
```

### 真题模式 → JSON

```json
{ "type", "level", "prompt", "points", "slices", "buildChain", "answerStructure", "extractedReference", "uncertainties", "warnings" }
```

## 四、练习接入

用户材料不混入 `SUMMARY_BANK`。练习时在内存构造临时 `tempQ` 对象，复用现有概括训练四步向导（通读→找点→归类→成句）。

历史记录额外保存 `source: 'user-material'` 和 `_materialId`。

## 五、代码位置索引

| 功能 | 位置 |
|------|------|
| CSS 样式 | `/* ============ 我的材料 ============ */` |
| 导航按钮 | `data-tab="materials"` |
| HTML 面板 | `#panelMaterials` |
| 数据层 CRUD | `USER_MATERIALS_KEY` |
| switchTab 扩展 | `switchTab` 内 `panelMaterials` |
| 材料管理模块 | `// USER MATERIALS MODULE` |
| initMaterialsModule 调用 | `initApp()` 内 |

## 六、第一版不做（保持不变）

- PDF、Word、图片 OCR
- 批量文件夹导入
- 云端账号和跨设备同步
- 公共题库发布
- 自动进入 SUMMARY_BANK
- 自动联网搜索材料来源
- 复杂富文本编辑器
- 新增后端

## 七、验收结果

- `node _dev/check.js` → 语法检查通过，当前全部 ID 引用均可解析
- `node _dev/_validate.js` → 无 ERROR（当前 11 条 WARNING 为内置题库既有提示，非新增）
- JS `new Function()` 语法校验通过
