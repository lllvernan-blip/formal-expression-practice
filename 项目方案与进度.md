# 规范表达练习：项目方案与进度

> 本文件是项目当前有效的产品方案、技术边界和进度记录。验收步骤单独见 [回归验收清单.md](回归验收清单.md)。旧版方案与实施记录存放在 `_archive/`，仅作历史查阅。

## 1. 项目目标

建立“读材料 → 找事实 → 排除干扰 → 归类 → 提炼上位概括 → 组织规范表达”的完整训练闭环。

成功标准不是页面能显示分数，而是做完一道题后能说清楚：漏掉了哪条事实，哪些事实应该归为同一点，概括词为什么准确或不准确，答案缺了什么。

当前采用单文件 HTML，不引入后端或构建工具：

- 主应用：`规范表达练习.html`
- 概括训练：不内置来源型材料；用户在“我的材料”中提供有权使用的材料，由 AI 生成后进入可练习状态，用户材料不写入 `SUMMARY_BANK`
- 概括训练样板和来源型材料草稿已清理；当前只保留主应用和用户材料导入流程
- 验收入口：`回归验收清单.md`

## 2. 当前产品结构

### 2.1 概括训练

五步向导：

1. 通读完整材料
2. 在材料中找出支撑答案的事实
3. 将事实归类并写出上位概括词
4. 根据要点和事实组织答案
5. 查看 AI 批改结果；自由划选的有效性只在提交时统一判断并进入错题本

离线题目使用 slice 数据模型。材料由连续 slice 拼回，slice 标注 `valid`、`point` 和干扰原因；采分点包含 `name`、`keys`、`acceptable`；`buildChain` 展示“事实 → 共同含义”的搭建过程。

### 2.2 规范改写

保留口语表达转正式表达的训练模式，题目和批改均由 AI 完成。它与概括训练共享应用外壳和 API 配置，但不混用答题状态。

### 2.3 我的材料

用户可以粘贴或上传材料，由 AI 生成可练习结构；也可以导入材料、题目和答案，由 AI 做结构化提取。

用户原文与 AI 加工结果分开保存，生成内容必须经过用户确认后再进入可练习状态。用户材料使用独立 localStorage：

```text
formal_expression_user_materials_v1
```

用户材料不覆盖、不改写、不混入内置题库。

## 3. 离线与在线边界

### AI 练习模式

概括训练材料生成后进入 AI 练习；题目、自由划选判断和提交批改均要求 API。规范改写题目和批改同样要求 API。项目不再提供内置来源型概括题库，也不提供离线替代结果。

- 找点步骤按难度使用预切片或在线自由划选
- 采分点、归类和完整答案由 AI 批改
- AI 请求失败时保留当前答案并提示重试，不生成离线替代结果

界面必须明确标注“AI 陪练 · 题目与批改均由 AI 完成”。

### AI 练习模式

正常练习统一要求 API 配置，AI 负责以下任务：

- AI 逐语义单元批改
- AI 出题和材料结构化提取
- AI 扫描文本并提出规范表达建议
- **自由划选找点**：仅 L3 高难度题在在线且已配置 API 时开放。通读时即可在同一份可独立滚动的连续材料中长按选中文字，直接点击材料下方的加入按钮；提交完整答案时由 AI 统一判断，结果随批改进入错题本，练习过程中不提前揭示采分点。
- **错题本自由复盘**：进入只练错题模式后，主导航可直接进入。用户先长按并点击下方按钮加入待复盘区，再主动点击“AI 判断复盘选段”；AI 结合本题错因解释该段为什么应该选或不应选，不修改练习选段，也不重复写入历史记录。
- **移动端操作**：L3 普通练习材料区域独立滚动，长按文字后在视口底部固定显示“加入练习选段”；错题复盘统一先点击“加入待复盘选段”，再由用户主动点击“AI 判断复盘选段”。不拦截系统复制/长按选择；“换一题”与训练模式保持同一工具行，减少首屏占用。

找点入口按难度互斥：L1/L2 统一使用预切片点击；L3 在线且已配置 API 时使用连续材料自由划选。通读阶段和自由选段阶段不调用 AI 判断，不提前替学习者给出采分事实。手机端通过 `selectionchange`、`touchend` 同步系统文字选择。

AI 返回必须是结构化 JSON；判断不确定时标记 `uncertain`，不能假装精确。JSON 不完整时自动重试一次；AI 失败时保留当前内容并提示重试，不生成离线替代结果。

## 4. 用户材料数据协议

核心对象：

```js
{
  id: "um_时间戳_随机串",
  mode: "material" | "true-question",
  status: "draft" | "ready" | "archived",
  title: "自定义标题",
  createdAt: "ISO",
  updatedAt: "ISO",
  original: {
    material: "原始材料",
    question: "原始题目",
    answer: "原始答案",
    files: { materialName, questionName, answerName }
  },
  generated: {
    type, level, domain, prompt, material, slices, points,
    buildChain, answerStructure, ref, aiAnalysis
  },
  provenance: {
    sourceType: "用户上传材料" | "用户导入真题",
    generatedByAI: true,
    userConfirmed: false,
    confirmedAt: null
  }
}
```

材料模式的 AI 输出至少包含：`title`、`type`、`level`、`prompt`、`material`、`slices`、`points`、`buildChain`、`answerStructure`、`ref`、`uncertainties`。

真题模式保留原始材料、题目和答案，AI 结果放入 `aiAnalysis`，不覆盖原文。

第一版暂不做：PDF/Word/图片 OCR、批量导入、云端同步、公共题库发布、自动联网找来源、复杂富文本编辑器和后端服务。

## 5. AI 生成质量约束

AI 生成的题目和答案必须满足：

- 参考答案使用“总括句 + 编号 + 概括词 + 具体事实”结构
- 每个要点都写清材料中的动作、对象或结果
- 概括词从材料事实提炼，具体事实优先保留材料原词
- 禁止只堆“整合资源、搭建平台、提升水平”等空泛词
- 不机械套用同一种“三段式”句型
- 不补写材料没有的目的、效果或因果关系
- 出现语病、搭配不当或宣传稿套话时，结果不能直接进入可练习状态
- 材料难度根据长度、采分点数量和事实密度校正，不把模型标签直接当作测量结果

用户材料生成后，页面要区分“原始材料”和“AI 加工结果”，并保留用户确认边界。

## 6. 数据与隐私边界

- 练习记录、API 配置和用户材料默认保存在当前浏览器 localStorage
- 用户材料发送给第三方模型前，应明确在线/API 状态；涉及隐私或未公开内容时不应默认上传
- API Key 不写入题库、材料草稿、日志、仓库或 Git 历史
- 用户材料不写入 `SUMMARY_BANK`
- `_inbox/` 是 WorkBuddy 采集的原始事实卡片，不覆盖、不删除、不移动
- 来源型材料草稿不进入仓库；用户材料只保存在当前浏览器的独立 localStorage 中
- 不碰 OB 的 `存/`、`收/`、`我/碎念/`、`我/计划/`

## 7. 当前进度

### 已完成

- 概括训练五步向导写入主应用
- 用户材料 AI 生成题目、确认后进入概括训练；离线 slice 规则反馈
- 规范改写 AI 出题、AI 批改和失败重试
- 我的材料：原文保存、AI 生成、真题结构化提取、确认后练习
- AI 生成协议和参考答案质量约束
- 生成中状态、返回当前题和旧请求结果隔离
- L3 在线材料自由划选找点入口：学习者通读时长按 → 点击材料下方按钮加入选段 → 提交时 AI 统一批改并进入错题本；L1/L2 保持预切片点击
- README、版权边界和验收清单已同步

### 当前验证状态

- `node _dev/check.js`：通过
- `node _dev/_validate.js`：无 ERROR、无 WARNING；校验提示区间已按当前叙事型训练材料调整
- `git diff --check`：通过
- 静态逻辑已按难度分流：L1/L2 显示预切片；L3 仅在线且有配置时显示自由划选；通读阶段不触发 AI 判断
- 本轮桌面浏览器回归：已完成；隔离浏览器无法访问 localhost，因此通过本地 file 页面验证；移动端完成 390×844 视口模拟，真机触摸仍需人工复核
- AI JSON 生成增加统一解析重试；规范改写与概括训练均不再回退到离线题库
- AI 请求改为按任务自动管理：普通请求和题目/复盘请求 45 秒，用户材料生成/解析 75 秒；输出上限按任务预算与模型最大输出取较小值，设置页不要求用户手动调整；材料切片校验支持忽略空白和常见标点差异，并在结构校验失败时自动重试一次
- 自由划选真实 API 端到端批改：尚未实测
- 真机触摸、输入法和真实手机视口：代码已补充触摸选择同步，仍需人工复核

## 8. 后续优先级

1. 用真实 API 测试 L3 自由划选提交：有效事实、背景/例子、跨逗号选段各至少一次；确认批改结果能进入错题本，且练习阶段不提前显示有效/无效判断。
2. 继续优化用户材料生成后的题型、难度和质量校验；长材料先按段落拆分，用户确认 AI 结果后才进入练习。
3. 观察不同 OpenAI 兼容接口对 JSON response_format 和长输出的兼容性。
4. 为用户材料增加归档、复练和错题重练入口，并观察是否能形成稳定复练闭环。
5. 重新评估 AI 判断结果的人工确认提示、成本提示和失败重试边界。

## 9. 变更与验收纪律

每次修改后按 [回归验收清单.md](回归验收清单.md) 执行。报告状态时严格区分：

- **已写入代码**：文件中已有实现
- **已通过静态检查**：语法、ID 或题库校验通过
- **已浏览器实测**：真实浏览器中完成操作
- **尚未实测**：只完成代码或静态验证

未经明确要求，不执行 commit、push、部署或公开发布。
